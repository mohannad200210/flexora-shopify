feat: Item 5 — add sticky add-to-cart bar using theme's native sticky_atc block

templates/product.json (sections.main.blocks.sticky_atc_bar):
- Reuses existing `sticky_atc` block type in sections/main-product.liquid (no new section file)
- Placed at the end of sections.main.block_order (last in list, as is convention for sticky bars)
- Settings:
  - function: add_to_cart (triggers the AJAX add-to-cart)
  - display_when: after_scroll (appears once user scrolls past the regular ATC button — no need to always show)
  - button_label: 'أضيفي للسلة' (feminine-form Arabic for the target audience)
  - custom button color: #0F766E (teal, matches the site color scheme)
  - image/title/price/variant picker: all shown on desktop and mobile
  - rating stars shown with our 742-review placeholder
  - separate picker mode for clearer size selection
- No new section file built
